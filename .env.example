# Copy to .env for local non-Docker runs. The docker-compose.yml has its own values.
APP_DATABASE_URL=postgresql+asyncpg://fleet:fleet@localhost:55432/fleet
APP_LOG_LEVEL=INFO
APP_ENVIRONMENT=dev
APP_CORS_ORIGINS=http://localhost:5173
